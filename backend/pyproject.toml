[build-system]
build-backend = "poetry.core.masonry.api"
requires = [
  "poetry-core>=1",
]

[tool.poetry]
name = "sounds-pathfinder"
version = "0.1.0"
description = ""
authors = ["PanMartinez <marcinplotka88@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.111.1"
sqlalchemy = "^2.0.31"
python-dotenv = "^1.0.1"
spotipy = "^2.24.0"

[tool.poetry.dev-dependencies]
pytest = "^8.3.2"
